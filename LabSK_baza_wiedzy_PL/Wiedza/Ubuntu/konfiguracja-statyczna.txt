# /etc/netplan/01-networkd.yaml
# Statyczna konfiguracja interfejsów na mszynach GE225:s?
# ato 2020
# Test: netplan –debug try –config-file /tmp/01-networkd.yaml

network:
  version: 2
  renderer: networkd
  ethernets:

    # Na interfejsie eno2 nadal DHCP:

    eno2:
      #critical: yes
      dhcp4: yes
      #dhcp4-overrides:                 # opcjonalne modyfikacje:
      #  route-metric: 100

    # Interfejs eno1 konfigurujemy statycznie:

    eno1:
      #dhcp4: true
      # this is plugged into a test network that is often
      # down - don't wait for it to come up during boot.
      #optional: true
      #mtu: 9000
      addresses:
        - 10.29.225.1/16
        - 172.29.225.1/24
      #gateway4: 10.20.30.1             # opcjonalnie bramka
      #nameservers:                     # i konfiguracja DNS
      #  search: [zet.pw.edu.pl, iem.pw.edu.pl]
      #  addresses: [10.29.213.3, 172.29.213.6]

  # Karta radiowa jeśli jest:

  wifis:
    wlp2s0:
      dhcp4: yes
      access-points:
        "ZET":
        #password: "Ha$se1K012345"
          auth:
            key-management: eap
            method: ttls
            anonymous-identity: "@internal.example.com"
            identity: "stud"
            password: "zetis-Pass-1234"

# Konfiguracja oryginalna ( 01-network-manager-all.yaml ) :
# Let NetworkManager manage all devices on this system
#network:
#  version: 2
#  renderer: NetworkManager
